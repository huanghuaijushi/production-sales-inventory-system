# AI 项目协作规则

## 1. 项目技术栈边界

本项目是前后端分离项目，技术栈如下：

- 后端：Spring Boot / Java / Maven 或 Gradle / MySQL
- 前端：Vue / JavaScript 或 TypeScript / Vite
- Android：Kotlin 或 Java / Gradle / Android Studio

除非我明确要求，否则不要主动查找、分析、创建或修改其他技术栈文件，例如：
- React
- Node.js 后端
- Python
- Go
- PHP
- Flutter
- UniApp
- 小程序
- 其他无关语言或框架

如果项目中存在这些文件，默认视为无关文件，不要处理。

---

## 2. 文件读取规则

默认只能读取我明确指定的文件。

如果你认为还需要读取其他文件，必须先说明：

1. 你想读取哪些文件；
2. 为什么需要读取；
3. 这些文件和当前问题的关系。

等我同意后，才能继续读取。

允许不需要确认的操作：

- 查看当前文件内容；
- 查看我明确指定的文件；
- 查看同一个功能模块下明显相关的文件，例如同一个 controller 对应的 service、mapper、entity；
- 查看项目根目录下的配置文件，例如 `pom.xml`、`build.gradle`、`package.json`、`vite.config.js`，但前提是当前问题确实需要。

---

## 3. 搜索规则

除非我明确要求，否则禁止进行全项目全文搜索。

禁止使用类似下面的全局搜索方式：

- `grep -r`
- `rg`
- `find .`
- 全项目关键词扫描
- 全项目文件内容搜索

如果确实需要搜索，必须先告诉我搜索范围，例如：

- 只搜索 `backend/src/main/java/com/xxx/order`
- 只搜索 `frontend/src/views/inventory`
- 只搜索 `android/app/src/main/java/com/xxx`

并且说明搜索关键词和目的。

我同意后才可以搜索。

---

## 4. 修改代码前必须先给方案

每次修改代码前，必须先输出修改方案，不要直接写代码。

修改方案必须包含：

1. 准备修改哪些文件；
2. 每个文件准备改什么；
3. 为什么这样改；
4. 是否会影响接口、数据库、页面、Android 调用；
5. 是否存在风险。

只有当我明确回复：

- “同意”
- “可以改”
- “开始写”
- “按这个改”

才允许修改代码。

如果我只是问“怎么改”“哪里有问题”“为什么报错”，只允许分析，不允许直接改代码。

---

## 5. 前后端分离规则

涉及接口时，必须同时考虑：

- 后端接口路径；
- 请求方法 GET / POST / PUT / DELETE；
- 请求参数；
- 返回字段；
- 前端调用位置；
- Android 是否也会调用这个接口。

不要只改后端不提醒前端影响，也不要只改前端不检查后端接口是否匹配。

如果修改接口返回结构，必须提醒我：

> 这个修改可能会影响 Vue 前端或 Android 端调用。

---

## 6. Spring Boot 后端规则

后端代码优先遵循现有结构，不要随意新建架构。

常见结构包括：

- controller
- service
- serviceImpl
- mapper / repository
- entity / model
- dto / vo
- config
- exception

不要随便引入新的复杂设计，例如：

- 微服务
- MQ
- Redis
- Elasticsearch
- DDD
- 事件驱动
- 复杂中间件

除非我明确要求。

修改数据库相关逻辑时，必须说明：

- 是否需要改表；
- 是否需要新增字段；
- 是否需要迁移数据；
- 是否影响旧数据；
- 是否影响库存、订单、生产、报工等核心业务。

---

## 7. Vue 前端规则

前端修改时，优先保持当前项目风格。

不要随意更换：

- UI 框架；
- 路由结构；
- 状态管理方式；
- 请求封装方式；
- 页面整体设计风格。

修改页面时，需要说明：

- 修改哪个页面；
- 修改哪个组件；
- 是否影响接口字段；
- 是否影响移动端适配；
- 是否影响现有交互流程。

---

## 8. Android 规则

Android 端只处理 Android 项目相关文件，例如：

- `app/src/main/java`
- `app/src/main/kotlin`
- `app/src/main/res`
- `AndroidManifest.xml`
- `build.gradle`
- `settings.gradle`

不要把 Android 问题误判成 Vue 前端问题。

如果 Android 调用后端接口，需要确认：

- baseUrl；
- 接口路径；
- 请求参数；
- 返回字段；
- token 或登录状态；
- 网络权限；
- 线程/协程/回调处理。

---

## 9. 输出方式

回答问题时，优先使用这种格式：

### 问题原因
说明为什么出问题。

### 涉及文件
列出相关文件。

### 修改思路
先讲方案，不直接改。

### 等待确认
最后必须问：

“你同意这个修改方案后，我再开始改代码。”

---

## 10. 禁止行为

除非我明确要求，否则禁止：

- 全项目搜索；
- 大范围重构；
- 新建无关文件；
- 引入新框架；
- 改动我没提到的功能；
- 自动格式化大量文件；
- 删除代码；
- 修改数据库结构；
- 修改接口返回格式；
- 改动 Android、前端、后端之外的无关文件。

如果必须做这些操作，必须先单独提醒我并等待确认。